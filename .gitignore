.DS_Store
Thumbs.db
node_modules/
*.log

# Claude Code local settings
.claude/

# Build output
dist/

# Local env (Supabase keys). The anon key is public by design, but env files
# shouldn't be committed — set these in .env.local and in the deploy environment.
.env
.env.local
.env.*.local

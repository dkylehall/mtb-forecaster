{
  "name": "mtb-forecaster",
  "version": "0.1.0",
  "description": "Mountain-bike trail forecaster: tells you when your riding areas are dry enough AND warm enough to ride, from past + forecast weather.",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "leaflet": "^1.9.4",
    "sortablejs": "^1.15.7",
    "vue": "^3.5.13"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.2.1",
    "vite": "^6.0.7",
    "vitest": "^2.1.8"
  }
}
